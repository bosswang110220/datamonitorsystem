<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wxy.data.dao.UserMapper">
    <resultMap id="user" type="com.wxy.data.entity.User"/>
    <parameterMap id="user" type="com.wxy.data.entity.User"/>
    <select id="findUserByUsername" parameterType="String" resultMap="user">
        SELECT * FROM user
        WHERE userName=#{userName}
    </select>


    <update id="updateUserByUsername" parameterMap="user">
        UPDATE USER SET userPassword=#{userPassword} WHERE userName=#{userName}
    </update>

    <delete id="deleteUserByUsername" parameterType="String">
        DELETE FROM USER WHERE userName=#{userName}
    </delete>

    <!-- 使用alias自定义的parameterType-->
    <insert id="saveUser" parameterType="user">
        INSERT INTO USER (userPassword,userName) VALUES (#{userPassword},#{userName})
    </insert>

    <select id="getUserList" resultMap="user">
        SELECT  * FROM USER
    </select>
</mapper>